import { SumFn } from "../types";

/**
 * Функция которая складывает числа
 * @param {number} a первое число
 * @param {number} b второе число
 * @returns {number} итоговое значение суммы
 */
export const sum : SumFn = (a, b) => a + b; 